图片资源目录
============

此目录用于存放系统所有图片资源文件。

当前文件：
- favicon.svg - 系统图标（浏览器标签页显示）

使用方式：
- 在 HTML 中引用：/images/favicon.svg
- 在 CSS 中引用：url('/images/文件名')
- 在 JavaScript 中引用：'/images/文件名'

支持的图片格式：
- SVG（推荐，矢量图，缩放不失真）
- PNG（适合复杂图片）
- JPG（适合照片）
- GIF（适合动画）

